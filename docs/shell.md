# 主页面（App Shell）

管理端全局壳：导航栏 + 内容区。各业务页（Streams / Previews / …）渲染于内容区；本页只定义 **布局 Mode**、**导航项**、**当前用户区** 与 **页面配置弹窗**。

UI 壳（下拉、弹窗）统一用 **Element Plus**。

| 概念 | 说明 |
|------|------|
| **Mode** | `sidebar`（侧栏）\| `topbar`（顶栏）；控制导航栏位置与形态 |
| **导航项** | Streams、Previews、Recordings、Models、Pipelines、Servers、Warnings、Users |
| **内容区** | 当前选中导航对应的业务页；各页自身标题 / 工具栏见对应 `*.md` |
| **页面配置** | 齿轮弹窗；目前仅 **Mode** |

默认 **Mode** 为 `sidebar`。仅在页面配置弹窗点击 **Apply** 后生效；已登录时经 [shell_api.md](./shell_api.md) **PUT /settings** 按用户持久化，刷新后恢复。

---

## 布局总览

### 侧栏模式（`sidebar`）

```
+----------+========================================================================+
| [Logo]   |                                                                        |
|          |  （内容区：当前业务页）                                                   |
| Streams  |                                                                        |
| Previews |                                                                        |
| Record.  |                                                                        |
| Models   |                                                                        |
| Pipeline |                                                                        |
| Servers  |                                                                        |
| Warnings |                                                                        |
| Users    |                                                                        |
|          |                                                                        |
| -------- |                                                                        |
| alice ⚙  |                                                                        |
+----------+========================================================================+
```

| 区域 | 说明 |
|------|------|
| 左侧栏 | 纵向导航；自上而下为各导航项；底部为用户名 + 齿轮 |
| 内容区 | 占满剩余宽度；业务页全高 |

### 顶栏模式（`topbar`）

```
+===================================================================================+
| Streams  Previews  Recordings  Models  Pipelines  Servers  Warnings  Users   alice ⚙ |
+===================================================================================+
|                                                                                   |
|  （内容区：当前业务页）                                                              |
|                                                                                   |
+===================================================================================+
```

| 区域 | 说明 |
|------|------|
| 顶栏 | 横向导航；左起为各导航项；**最右侧**为用户名 + 齿轮 |
| 内容区 | 顶栏下方全宽；业务页全高 |

---

## 导航栏

### 导航项

固定顺序（两种 Mode 相同）：

| 顺序 | 标签 | 对应文档 / 页面 |
|------|------|-----------------|
| 1 | Streams | [stream.md](./stream.md) |
| 2 | Previews | [preview.md](./preview.md) |
| 3 | Recordings | [recordings.md](./recordings.md) |
| 4 | Models | [model.md](./model.md) |
| 5 | Pipelines | [pipeline.md](./pipeline.md) |
| 6 | Servers | [server.md](./server.md) |
| 7 | Warnings | Warnings 页（待定） |
| 8 | Users | Users 页（待定） |

| 行为 | 说明 |
|------|------|
| 点击导航项 | 切换内容区路由 / 页面；当前项高亮 |
| 高亮 | 仅一项；与当前路由一致 |
| 文案 | 固定英文标签如上表；侧栏可完整展示，窄侧栏时允许省略号 |

侧栏模式下导航项**纵向**排列；顶栏模式下**横向**排列，项间距紧凑，过窄时可横向滚动（导航项不换行）。

### 用户区（栏尾）

位于导航项之后（侧栏：**栏底**；顶栏：**栏最右**）。

| 元素 | 说明 |
|------|------|
| **用户名** | 当前登录用户显示名（如 `alice`）；只读文本 |
| **齿轮 ⚙** | 用户名**右侧**；点击打开 [页面配置弹窗](#页面配置弹窗) |

无登录态时用户名占位为 `—`；齿轮仍可用，Mode 仅会话临时（默认 `sidebar`），见 [shell_api.md](./shell_api.md)。

---

## 页面配置弹窗

点击齿轮打开。标题 **Page Settings**（或中文「页面配置」；实现时与产品文案统一）。

打开时以**当前已应用**配置填充表单（草稿）；弹窗内修改**不**改变壳布局，直至 **Apply**。

关闭且丢弃草稿：右上角 `×` / 点遮罩 / `Esc` / **Cancel**（与 Cancel 同效）。

```
+------------------------------------------+
| Page Settings                        [x] |
+------------------------------------------+
| Mode     [ sidebar          ▼ ]          |
|                                          |
|                    [ Cancel ] [ Apply ]  |
+------------------------------------------+
```

| 字段 | 控件 | 选项 | 说明 |
|------|------|------|------|
| **Mode** | 下拉（Element Plus `ElSelect`） | `sidebar` / `topbar` | 展示文案：**侧栏** / **顶栏**（或 Sidebar / Topbar）；仅改弹窗草稿 |

| 按钮 | 样式 | 行为 |
|------|------|------|
| **Cancel** | 次按钮 | 丢弃草稿；关弹窗；壳布局与已持久化配置**不变** |
| **Apply** | 主按钮 | 将草稿**应用**到壳；已登录则 **PUT /settings**；成功后关弹窗 |

| 行为 | 说明 |
|------|------|
| 改 Mode（下拉） | 只更新弹窗草稿；**不**调 API；**不**切换壳 |
| **Apply** 成功 | 壳按新 Mode 重排；内容区路由与业务页状态**保留** |
| **Apply** 失败 | 壳不切换；弹窗保持打开；Toast 错误（已登录写库失败时） |
| 未登录 **Apply** | 仅会话内应用草稿；**不**调 `PUT /settings` |

当前弹窗**仅**含 Mode；后续配置项追加到同一弹窗，仍经 **Apply** 一次性提交。

---

## Mode 切换行为

| 项 | 说明 |
|----|------|
| 生效时机 | 仅 **Apply**；下拉变更本身不生效 |
| 应用瞬间 | 导航从侧栏 ↔ 顶栏重排；内容区路由与业务页状态**保留** |
| 当前高亮 | 应用后仍指向同一导航项 |
| 默认值 | 无已存配置时为 `sidebar`（`DEFAULT_MODE`） |
| 持久化 | 已登录且 **Apply**：后端 `/settings`；未登录 **Apply**：仅会话临时 |

---

## 与业务页关系

| 项 | 说明 |
|----|------|
| 业务页标题 | 各页自有顶栏标题（如 Streams、Preview）；**不属于**本壳导航 |
| Preview 等页内「无齿轮」 | 指业务页工具栏；**壳层**齿轮在导航用户区，二者不冲突 |
| 路由 | 每导航项对应一条前端路由；深链进入时壳按路由高亮对应项 |

---

## 交互流程

```
进入管理端           ──> GET /me + GET /settings；渲染壳；按路由高亮导航
点击导航项           ──> 切换内容区；高亮该项
点击齿轮             ──> 打开 Page Settings；表单填入当前已应用值（草稿）
Mode 下拉改值        ──> 仅改草稿；壳不变；不调 API
Cancel / × / Esc     ──> 丢弃草稿；关弹窗；壳不变
Apply                ──> 应用草稿到壳；已登录则 PUT /settings；成功后关弹窗
```
