---
description: 后端代理 DeepStream REST API 的规则 — 禁止自建流管理，必须转发内置 API
globs: backend/**/services/deepstream*.py, backend/**/services/ds_*.py
alwaysApply: false
---

# DeepStream 代理规则

## 核心原则

后端是 DeepStream 的**代理**，不是替代。视频流管理必须通过转发 DeepStream 内置 REST API 实现。

## DeepStream 内置端点

```
POST http://deepstream:9000/api/v1/stream/add        # 添加视频流
POST http://deepstream:9000/api/v1/stream/remove      # 移除视频流
GET  http://deepstream:9000/api/v1/stream/get-stream-info   # 查询流状态
GET  http://deepstream:9000/api/v1/health/get-dsready-state # 健康检查
```

## 代理实现

使用 `httpx.AsyncClient` **长连接池单例**，配合 Django async view 调用：

```python
# ✅ 正确：单例连接池 + 分级超时
class DeepStreamClient:
    def __init__(self):
        self._client = httpx.AsyncClient(
            base_url=settings.DEEPSTREAM_REST_URL,
            timeout=httpx.Timeout(connect=5.0, read=10.0, write=5.0, pool=5.0),
        )

    async def add_stream(self, camera_id: str, camera_name: str, rtsp_url: str):
        return await self._client.post("/api/v1/stream/add", json={
            "key": "sensor",
            "value": {
                "camera_id": camera_id,
                "camera_name": camera_name,
                "camera_url": rtsp_url,
                "change": "camera_add",
            }
        })

deepstream_client = DeepStreamClient()  # 模块级单例

# ❌ 禁止：每次请求新建 Client（无连接池复用，FD 泄漏风险）
async def add_stream(...):
    async with httpx.AsyncClient(timeout=10.0) as client:
        ...
```

## 禁止事项

- **禁止**在后端导入 `pyservicemaker` 或操作 GStreamer pipeline
- **禁止**自建 RTSP 拉流、解码、推理等逻辑
- **禁止**绕过 DeepStream REST API 直接操作其内部状态

## 容错

- 让 `httpx` 异常（`TimeoutException`、`ConnectError`）自然抛出，由 DRF 全局异常处理器捕获并返回明确错误
- 添加流失败时异常会中断事务，确保数据库中 Camera 状态不会处于脏状态（使用 `transaction.atomic`）
- 不要在 proxy 层 try-except 吞掉异常
