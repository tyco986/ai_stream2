---
description: 全局代码风格 — 适用于项目所有 Python 代码
alwaysApply: true
---

# 代码风格

## 异常处理

不要随意使用 try-except，让异常自然抛出以便调试。只在以下场景允许 try：
- 外部 I/O 有明确恢复策略（如 HTTP 重试、Kafka 重连）
- 框架要求捕获（如 DRF 全局异常处理器）

```python
# ✅ 让异常自然抛出
def get_camera(camera_id):
    return Camera.objects.get(id=camera_id)

# ❌ 吞掉异常
def get_camera(camera_id):
    try:
        return Camera.objects.get(id=camera_id)
    except Camera.DoesNotExist:
        return None
```

## 禁止 global

禁止使用 `global` 关键字。需要共享状态时使用类属性、依赖注入或 Django settings。

## import

所有 import 必须置于文件开头，按以下顺序分组，组间空一行：
1. 标准库
2. 第三方库
3. Django / DRF
4. 项目内部模块

禁止函数内部 import（除非解决循环依赖，且必须加注释说明原因）。

## 命名

函数名、类方法名、参数名均禁止前置 `_`。

```python
# ✅ 无前缀下划线
def get_camera(camera_id):
    ...

class AlertEngine:
    def calculate_score(self, detection):
        ...

# ❌ 前置下划线
def _get_camera(_camera_id):
    ...

class AlertEngine:
    def _calculate_score(self, detection):
        ...
```

## 面向对象

优先使用类封装逻辑，便于复用和测试。Service 层、Client 层必须是类，不要写散落的函数集合。

`__init__` 方法中首先要做的是将全部参数赋值给 `self`，再写其他逻辑。

```python
# ✅ 类封装，可复用可测试
class DeepStreamClient:
    def __init__(self, base_url, timeout):
        self.base_url = base_url
        self.timeout = timeout

    async def add_stream(self, camera_id, rtsp_url):
        ...

# ❌ 散落函数，难以复用和 mock
async def add_stream_to_deepstream(camera_id, rtsp_url):
    base_url = settings.DEEPSTREAM_URL
    ...

# ❌ __init__ 中先做其他逻辑，或遗漏参数赋值
class DeepStreamClient:
    def __init__(self, base_url, timeout):
        self.client = httpx.AsyncClient(timeout=timeout)
        self.base_url = base_url
```

## 语言

代码中除注释外一律使用英文（变量名、函数名、类名、字符串 key）。
注释使用中文或英文均可，但同一文件内保持一致。

## 废话代码（禁止补丁式堆叠）

「废话」指冗余实现：在旧代码上叠 if、回退路径、中间方法，而不是按新需求整理形状。

- 加功能前先读现有结构；能重构就重构，不顺手就删旧路径再写，不要双轨并存
- 新方案取代旧方案时，同步删掉失效常量、方法和分支（如已弃用的解析路径）
- 同一职责只保留一条路径；不要「先 A 不行再 B 再 C」的层层回退，除非边界上真有多种来源
- 改完自问：若从零实现这功能，还会是现在这个文件形状吗？若否，继续删

```python
# ✅ 新 API 接受目录 → 直接改构造与调用方
class YoloPipeline:
    def __init__(self, config_dir, pipeline_name):
        self.config_dir = Path(config_dir)
        ...

# ❌ 补丁：保留旧 yaml 路径，再叠 config_dir 分支、resolve 回退、is_file 守卫
def resolve_meta_via_pgie(...):
    ...
def resolve_meta_via_params(...):
    ...
```

## 简洁

- 避免不必要的 if 分支，善用字典映射、`or` 默认值
- 不要写只有一个分支的 if-else

```python
# ✅ 单出口，用变量承接结果
def get_status_label(status):
    label = STATUS_MAP.get(status, "未知")
    if status == "offline":
        label = "离线"
    return label

# ❌ 多余的 else
def get_status_label(status):
    if status == "offline":
        label = "离线"
    else:
        label = STATUS_MAP.get(status, "未知")
    return label
```

## 函数返回

函数或方法若**有返回值**，`return` **有且仅有一个**，且必须放在末尾；分支逻辑通过修改变量表达，禁止中途 `return`（含早返回）。无返回值时可隐式结束，不必写 `return`。

```python
# ✅ 先定义结果变量，分支中修改，末尾唯一 return
def get_label(status):
    label = STATUS_MAP.get(status, "未知")
    if status == "offline":
        label = "离线"
    return label

# ✅ 无返回值，可隐式结束
def log_event(event):
    logger.info(event)

# ❌ 中途 return
def get_label(status):
    if status == "offline":
        return "离线"
    return STATUS_MAP.get(status, "未知")
```

## 推导式

尽量使用列表推导式、字典推导式（及集合推导式）替代等价的 `for` 循环构建容器；逻辑复杂、含副作用或需多步累积时仍用普通循环。

```python
# ✅ 列表推导式
labels = [obj.label for obj in objects if obj.label]

# ✅ 字典推导式
scores = {det.id: det.confidence for det in detections}

# ❌ 仅为构建列表而写的循环
labels = []
for obj in objects:
    if obj.label:
        labels.append(obj.label)
```

## 禁止嵌套函数定义

不要在函数内部定义函数。需要辅助逻辑时提取为类方法或模块级函数。

```python
# ✅ 提取为方法
class AlertEngine:
    def evaluate(self, detection):
        score = self.calculate_score(detection)
        ...

    def calculate_score(self, detection):
        ...

# ❌ 函数内定义函数
def evaluate(detection):
    def calculate_score(d):
        ...
    score = calculate_score(detection)
```

## 复用

- 重复逻辑超过两次必须提取为公共方法或 mixin
- 跨 app 的公共逻辑放 `services/` 或 `utils/`
- DRF 通用行为用 mixin 封装，不要在每个 ViewSet 里重复
