# bash servers/generator/scripts/3_generate.sh --config <template_name>
#
# Artifacts: pipeline.yml, pgie.yml, params.yml, ... under config_save_dir.

generator: YoloPoseImageConfigGenerator
config_save_dir: /root/configs/generator/yolo11n_pose_image
pgie_model_dir: models/trt/yolo11n-pose
input: attachments/images/test.png
output: outputs/deepstream/yolo11n_pose_image.jpg
enable_kafka: true
pgie_class_attr:
  all:
    conf: 0.25
