# Prerequisite: servers/generator/scripts/3_generate.sh --config topdown_pose_image

type: RtmposeImagePipeline
name: topdown-pose-image
config_dir: /root/configs/generator/topdown_pose_image
drawer:
  show_label: true
  show_conf: true
logger:
  interval: 0
messager:
  topic: deepstream-detections
  host: ai_stream2_kafka
  port: 9092
